server:
  port: 8898
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MingmingYue/cloud-study
          searchPaths: config-repository/{application}/
          force-pull: true
      #          username:
      #          password:
      label: master

eureka:
  client:
    serviceUrl:
      defaultZone: http://10.10.35.236:8761/eureka/