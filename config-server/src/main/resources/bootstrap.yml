server:
  port: 8898
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
#          uri: https://github.com/meteor1993/SpringCloudLearning # git仓库的地址
#          search-paths: chapter6/springcloud-config  # git仓库
          uri: https://github.com/MingmingYue/cloud-study
          search-paths: configRepository/order
          force-pull: true
#          username:
#          password:
      #   label: master

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8761/eureka/